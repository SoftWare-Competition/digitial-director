# 服务器部署文档

> 日期: 2026-07-08 | 域名: https://xinphotoshare.click

---

## 一、服务器信息

| 项 | 值 |
|------|------|
| IP | 47.118.21.150 |
| 系统 | Ubuntu 22.04.5 LTS |
| Python | 3.10.12 |
| Web服务器 | Nginx 1.18.0 |
| 框架 | FastAPI + Uvicorn |
| SSL | Let's Encrypt, 到期 2026-08-16 |

---

## 二、目录结构

项目根目录: /opt/

backend/                   FastAPI后端
  app/
    main.py                入口,路由注册,静态文件挂载
    config.py              pydantic_settings, 从.env读取
    routers/qa.py          问答API /api/v1/qa/ask (含JWT auth)
    services/
      qa_service.py        ASR→LLM→TTS流水线 (ASR失败自动降级)
      llm_client.py        DeepSeek LLM + RAG检索 + _clean_answer过滤
      speech_client.py     阿里云 NLS ASR + Edge TTS
    models/schemas.py      Pydantic数据模型
  static/model/            Live2D模型文件
  data/knowledge_base.json 知识库 (44KB)
  tests/                   测试文件 + test_question.mp3
  .env                     环境变量(含密钥，敏感!)
  lingshan.db              SQLite数据库 (WAL模式)

build/live2d-viewer/       Live2D前端资源
  index.html               主页面(渲染+动画+语音对话UI)
  pixi.min.js              456KB
  live2dcubismcore.min.js  150KB
  cubism4.min.js           120KB
  index.min.js             127KB

---

## 三、Nginx配置

文件: /etc/nginx/sites-enabled/photo-gallery

核心路由:

| 路径 | 处理 | 说明 |
|------|------|------|
| /live2d-viewer/static/* | Nginx alias | JS文件,缓存30天 |
| /static/model/* | Nginx alias | 模型文件,缓存30天 |
| /api/v1/* | uvicorn :8001 | FastAPI后端 (lingshan) |
| /live2d-viewer | uvicorn :8001 | 返回index.html |
| /api/* | Node :3000 | 图片画廊API |

HTTP(80) 强制301到 HTTPS(443)

---

## 四、服务进程

### 启动

```
cd /opt/backend
nohup python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8001 > /tmp/uv.log 2>&1 &
```

### 重启

```
pkill -f "uvicorn app.main"
sleep 2
cd /opt/backend && nohup python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8001 > /tmp/uv.log 2>&1 &
```

### 查看日志

```
tail -f /tmp/uv.log
```

---

## 五、环境变量

文件: /opt/backend/.env

```
DEEPSEEK_API_KEY              DeepSeek API 密钥
DEEPSEEK_BASE_URL             https://api.deepseek.com
ALIBABA_ACCESS_KEY_ID         阿里云 AccessKey
ALIBABA_ACCESS_KEY_SECRET     阿里云 AccessKey Secret
ALIBABA_ASR_APPKEY            阿里云 NLS 项目 AppKey
DATABASE_URL                  sqlite:///./lingshan.db
```

config.py 通过 pydantic_settings.BaseSettings 自动从 .env 读取，代码不硬编码密钥。

---

## 六、外网验证

| URL | 预期 |
|-----|------|
| https://xinphotoshare.click/live2d-viewer | 显示数字人 + 语音对话 |
| https://xinphotoshare.click/api/v1/health | {"status":"ok"} |
| https://xinphotoshare.click/docs | Swagger API文档 |

curl测试:

```
# 健康检查
curl -sk https://xinphotoshare.click/api/v1/health

# 文字问答
curl -sk -X POST https://xinphotoshare.click/api/v1/qa/ask \\
  -F "text=你好" -F "audio=@/opt/backend/tests/test_question.mp3"

# 语音问答 (阿里云ASR)
curl -sk -X POST https://xinphotoshare.click/api/v1/qa/ask \\
  -F "audio=@/opt/backend/tests/test_question.mp3"
```

---

## 七、资源加载

| 资源 | 大小 |
|------|------|
| index.html | 21KB |
| pixi.min.js | 456KB |
| cubismcore.min.js | 150KB |
| cubism4.min.js | 120KB |
| index.min.js | 127KB |
| model.bin | 3.1MB |
| texture_00.png | 3.8MB |
| physics3.json | 64KB |
| cdi3.json | 13KB |
| 总计 | ~7.8MB |

JS和模型文件设置30天浏览器缓存

---

## 八、维护备忘

### 日志
```
tail -f /tmp/uv.log            # uvicorn 日志
tail -f /var/log/nginx/access.log  # Nginx 访问日志
```

### SSL续期 (到期2026-08-16)
```
certbot renew && systemctl restart nginx
```

### 数据库
```
# WAL checkpoint (减少文件大小)
sqlite3 /opt/backend/lingshan.db "PRAGMA wal_checkpoint(TRUNCATE);"
```

### 备份
```
tar -czf /opt/lingshan-backup-$(date +%Y%m%d).tar.gz /opt/backend /opt/build
```
