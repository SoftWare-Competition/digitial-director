# 数字人显示与发音机制

> 日期：2026-07-07 | 模型：音-免费版 (逐尾鲨, Cubism 3.0)

---

## 一、数字人显示机制

### 架构

```
微信小程序 / 浏览器
        |
   <web-view> / <iframe>
        |
https://xinphotoshare.click/live2d-viewer
        |
      Nginx
  ├─ /live2d-viewer        → uvicorn :8001 (index.html)
  ├─ /live2d-viewer/static/ → JS库 (pixi.js / cubism4 / live2d-display)
  └─ /static/model/         → 模型文件 (model.bin / texture / physics)
```

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 渲染引擎 | PixiJS v7 | 2D WebGL |
| Live2D核心 | Cubism 4 Core (WASM) | 官方运行时 |
| 桥接层 | pixi-live2d-display + cubism4插件 | PixiJS ↔ Cubism |
| 模型格式 | MOC3 (model.bin, 3.1MB) | 编译模型 |
| 纹理 | texture_00.png (3.8MB) | 8192×8192贴图 |
| 物理 | model.physics3.json (64KB) | 头发/衣物摆动 |
| 显示信息 | model.cdi3.json (13KB) | 参数与部件布局 |

### JS加载顺序(必须严格)

```
1. pixi.min.js               → PIXI 全局对象
2. live2dcubismcore.min.js   → Live2DCubismCore 全局对象
3. cubism4.min.js            → 注册 Cubism4Model 到 PIXI.live2d
4. index.min.js              → 提供 Live2DModel
```

### 模型初始化步骤

1. 检查依赖 (PIXI / Live2DCubismCore / PIXI.live2d)
2. 创建 PIXI.Application (透明背景, 自适应窗口, WebGL)
3. `PIXI.live2d.Live2DModel.from("/static/model/model.model3.json")`
   - 解析 model3.json → 加载 model.bin / texture / physics3 / cdi3
4. 模型添加至 stage, scale × 0.8 居中
5. PIXI.Ticker 渲染循环 (~60fps)

### 动画系统

**呼吸动画** (每帧):
```
ParamBreath = (sin(breathPhase) + 1) / 2
breathPhase += 0.03
```

**眨眼动画** (4阶段状态机):
```
Phase 0: 等待 3~6秒 → Phase 1: 闭眼4帧 → Phase 2: 保持1帧 → Phase 3: 睁眼4帧 → 回到Phase 0
```

**情绪控制** (7种):

| 情绪 | EyeSmile | BrowLY/RY | 触发时机 |
|------|----------|-----------|----------|
| happy | +0.8 | -0.5 | 回答完成后 |
| curious | 0 | -0.3 | 收到问题时 |
| surprised | 0 | -0.8 | — |
| concerned | 0 | +0.5 | — |
| thinking | 0 | L+0.2 R-0.3 | — |
| neutral | 0 | 0 | 播放结束重置 |

**用户交互**:
- 鼠标滚轮 → 缩放
- 单指拖动 → 平移
- 双指 → 缩放(0.4x~2.5x)
- 双击 → 重置视角

### 模型文件清单

| 文件 | 大小 | 用途 |
|------|------|------|
| model.model3.json | 434B | 入口配置 |
| model.bin | 3.1MB | MOC3二进制 |
| texture_00.png | 3.8MB | 角色贴图 |
| model.physics3.json | 64KB | 物理参数 |
| model.cdi3.json | 13KB | 显示信息 |
| model.vtube.json | 13KB | VTube兼容 |

---

## 二、数字人发音机制

### 完整流水线

```
用户输入(文字/语音)
        |
POST /api/v1/qa/ask
        |
qa_service.process_question()
  ├─ ① 文字输入 → 跳过ASR, 直接用
  ├─ ① 语音输入 → ASR转文字 (当前MOCK)
  ├─ ② RAG知识检索 (8维度, 44KB知识库)
  ├─ ③ DeepSeek LLM 生成回答 (≤150字)
  └─ ④ Edge TTS 合成MP3 → base64
        |
返回 { question_text, answer_text, answer_audio_base64 }
        |
前端 index.html
  ├─ startSpeak() → 口型循环动画
  ├─ new Audio(base64) → 播放MP3
  └─ onended → stopSpeak() + setEmotion('neutral')
```

### 语音输入: ASR (当前MOCK)

```python
# speech_client.py
async def speech_to_text(audio_bytes):
    if len(audio_bytes) < 200:
        return ("", 0.0)
    # TODO: 接真实ASR
    return ("[语音消息]", 0.85)
```

当前语音输入无法真正识别内容,统一返回"[语音消息]"。

前端录音: MediaRecorder API → audio/webm → FormData上传。

### 语音输出: Edge TTS

```python
# speech_client.py
async def text_to_speech(text):
    voice = "zh-CN-XiaoyiNeural"    # 晓依 - 温柔女声
    communicate = edge_tts.Communicate(text, voice)
    await communicate.save(tmp_path)  # 生成临时MP3
    return audio_bytes
```

| 参数 | 值 |
|------|------|
| 引擎 | Microsoft Edge TTS (免费) |
| 当前语音 | zh-CN-XiaoyiNeural (晓依) |
| 可换语音 | Xiaoxiao/Yunxi/Yunjian (普通话), HsiaoChen/HsiaoYu (台湾), HiuMaan (粤语) |
| 输出 | MP3 → base64 |

### 前端播放与口型

```js
// Edge TTS MP3 (主方案)
new Audio('data:audio/mp3;base64,' + base64).play()

// 浏览器TTS (兜底, 音质差)
speechSynthesis.speak(new SpeechSynthesisUtterance(text))
```

**口型同步** (8帧循环, 120ms/帧):

```js
var seq = [1.0, 0.65, 0.35, 0.65, 1.0, 0.0, 0.35, 0.65]
setInterval(() => setMouth(seq[i++ % 8]), 120)
```

注意: 口型是固定循环, 与音频内容无关。

### AI问答: DeepSeek LLM

| 参数 | 值 |
|------|------|
| 模型 | deepseek-chat |
| API | https://api.deepseek.com/v1/chat/completions |
| Temperature | 0.7 |
| Max Tokens | 400 |
| 知识库 | data/knowledge_base.json (44KB, 6模块) |
| 兜底 | API不可用时本地关键词匹配 |

### API Key管理

```
.env → config.py(pydantic_settings) → settings.deepseek_api_key → HTTP Header
```

密钥不在代码中硬编码, `.env` 不提交Git。

### 已知问题

| 问题 | 严重 | 说明 |
|------|------|------|
| ASR为MOCK | 严重 | 语音输入无法识别 |
| 口型不同步 | 中等 | 固定循环,与音频不匹配 |
| TTS阻塞 | 中等 | 等整个MP3生成完毕才返回 |

---

## 三、对外接口 (window全局方法)

```js
// 情绪
window.setEmotion('happy|surprised|concerned|curious|thinking|neutral')

// 口型
window.startSpeaking()   // 循环动画
window.stopSpeaking()    // 闭嘴
window.setMouthOpen(0~1) // 手动控制

// 视角
window.zoomIn()          // 放大
window.zoomOut()         // 缩小
window.resetView()       // 重置

// 模型
window._live2dModel      // 模型实例
```

## 四、加载状态流程

```
初始化中 → PixiJS v7.x → CubismCore OK → pixi-live2d-display OK
→ 创建画布 → 加载模型 → 加载成功 → 就绪(1秒后自动消失)
```
