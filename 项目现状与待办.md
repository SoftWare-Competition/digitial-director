# 灵山胜境 AI 数字人导游 - 项目现状与待办

> 更新日期：2026-07-10
> 当前分支：MiniProgram
> 服务地址：https://xinphotoshare.click
> 本地后端：http://localhost:8000（仅用于浏览器调试 Live2D 页面）
> 后端版本：FastAPI 0.1.0

---

## 一、项目结构

backend/          - FastAPI 后端 (Python + SQLAlchemy + SQLite)
  app/main.py     - FastAPI 入口，路由注册 + Live2D 静态文件挂载
  app/config.py   - .env 配置读取 (微信/DeepSeek/阿里云/和风天气)
  app/database.py - SQLAlchemy 引擎 + Session 工厂
  app/models/     - ORM 模型 (User, ScenicSpot, Route, Checkin, QASession, QAMessage)
  app/routers/    - API 路由 (user/qa/narration/spots/tips)
  app/services/   - 业务逻辑 (llm_client/qa_service/speech_client/wechat_client)
  app/utils/      - 工具函数 (get_user_id/geo)
  data/           - 静态数据 (knowledge_base.json/routes.json/spots.json)
  static/model/   - Live2D 模型文件

miniprogram/      - 微信小程序
  pages/
    index/        - 首页 (智能提示列表)
    profile/      - 个人中心 (登录/资料/历史)
    digital-human/- 数字人问答页 (web-view 嵌入 Live2D)
    routes/       - 路线选择
    map/          - 地图 + GPS 签到
    spot-detail/  - 景点详情

build/
  live2d-viewer/  - WebView HTML (PixiJS + Live2D + 聊天 UI)

## 二、已完成功能

### 2.1 用户系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 微信登录 | OK | wx.login() → 后端调微信 jscode2session API 换真实 openid |
| 手机号授权 | OK | open-type="getPhoneNumber" 弹出微信官方授权框，后端解密获取真实手机号 |
| 降级兼容 | OK | 微信未配置时自动 fallback 到游客模式，手动设置资料 |
| Token 签发 | OK | HMAC-SHA256 签名，30 天有效期 |
| Token 校验 | OK | POST /user/check-token |
| 更新资料 | OK | PUT /user/profile 修改昵称/头像 |
| 游客模式 | OK | 未登录时 user_id = anonymous，可正常使用全部功能 |
| 登录态恢复 | OK | 启动时检查本地 token 有效性，自动恢复或进入游客模式 |

### 2.2 用户数据绑定

| 功能 | 状态 | 说明 |
|------|------|------|
| QA 对话归属 | OK | QASession.user_id 绑定真实用户，不再是写死的 anonymous |
| 签到记录归属 | OK | Checkin.user_id 绑定真实用户 |
| 对话历史查询 | OK | GET /user/history 返回签到 + QA 会话列表 |
| 签到冷却按用户独立 | OK | 同一景点 30 分钟冷却按用户隔离 |

### 2.3 数字人问答

| 功能 | 状态 | 说明 |
|------|------|------|
| Live2D 模型渲染 | OK | PixiJS + pixi-live2d-display，支持缩放/拖拽/双击复位 |
| 眨眼/呼吸动画 | OK | 自动循环，眨眼间隔随机 3-6 秒 |
| 表情切换 | OK | happy/surprised/concerned/curious/thinking/neutral |
| 口型同步 | OK | 播放语音时自动驱动嘴部参数 |
| 文字输入问答 | OK | WebView 内输入文字发送 |
| 语音输入问答 | OK | WebView 录音(web) + 小程序接管录音(miniprogram) |
| 阿里云 Paraformer ASR | OK | 语音→文字识别，接入阿里云 Paraformer-v1 |
| Edge TTS 语音合成 | OK | 免费，神经网络语音 zh-CN-XiaoyiNeural |
| 对话连续性 | OK | session_id 持久化到 localStorage，刷新不丢上下文 |
| 问答反馈 | OK | POST /qa/feedback 点赞/踩 |

### 2.4 AI 智能问答

| 功能 | 状态 | 说明 |
|------|------|------|
| DeepSeek LLM | OK | deepseek-chat 模型，回答限制 150 字内 |
| RAG 知识检索 | OK | 8 维度关键词匹配，从 44KB 知识库精准抽取 |
| 多轮对话上下文 | OK | 最近 10 条历史消息注入 system prompt |
| 位置感知 | OK | GPS 坐标 -> 地理围栏匹配 -> 注入景点上下文 |
| 兜底机制 | OK | LLM 不可用时，本地知识库关键词匹配 |

### 2.5 首页

| 功能 | 状态 | 说明 |
|------|------|------|
| Hero Banner | OK | Logo + 景点/路线/AI统计 + 天气角标 |
| 快捷入口 2x2 | OK | 景点地图/游览路线/AI问答/我的 |
| 精选景点横滑 | OK | 5 张实拍图卡片，点击进详情 |
| 游览贴士 | OK | 开放时间 + 建议游览时长 |
| 查看全部定位 | OK | 跳转地图自动居中到景点区域 |

### 2.6 景点详情

| 功能 | 状态 | 说明 |
|------|------|------|
| 语音讲解播放器 | OK | 始终显示，Edge TTS 生成 MP3，缓存 24h |
| 播放/暂停 | OK | audio-player 组件 |
| 进度条 + 时长 | OK | 外部传入预估时长，播放时真实修正 |
| 灵山大佛数据 | OK | 规模/参观信息/文化内涵/详细介绍/讲解文稿已补全 |

### 2.7 景区服务

| 功能 | 状态 | 说明 |
|------|------|------|
| 22个景点数据 | OK | 含 GPS/规模/文化/拍照/参观信息 |
| 5 张景点实拍图 | OK | `miniprogram/images/spots/` (大照壁/九龙灌浴/百子戏弥勒/大佛/五印坛城) |
| 3条预设路线 | OK | 历史文化(6h) / 自然风光(5h) / 亲子家庭(4h) |
| GPS 签到讲解 | OK | 地理围栏 30m，自动触发语音讲解 |
| 智能提示 | OK | 时间提醒 + 天气提示 + 游览建议 |

## 三、待解决问题

### 3.1 个性化推荐引擎 [核心待办]

当前状态：三条路线是静态的，用户手动选择。LLM 的 system prompt 未注入用户兴趣偏好。

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 用户兴趣采集 | 高 | 首次使用时弹窗询问：历史/自然/亲子/美食 |
| User 表加 interests 字段 | 高 | 存储用户兴趣标签(JSON 数组)，如 [history, nature] |
| 兴趣保存接口 | 高 | PUT /user/preferences |
| 个性化路线推荐 | 高 | GET /routes/recommend?user_id=xxx |
| LLM 偏好注入 | 高 | System prompt 动态加入：游客偏好xxx，侧重xxx角度讲解 |
| 兴趣感知 RAG | 中 | _retrieve_knowledge() 根据偏好调整知识块检索权重 |

### 3.2 对话历史列表 (类似 DeepSeek/豆包)

当前状态：WebView 内只有当前会话的消息气泡，无历史对话列表。

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 会话列表 API | 高 | GET /qa/sessions (按用户列出所有会话摘要) |
| 新建会话 API | 高 | POST /qa/sessions |
| 删除会话 API | 中 | DELETE /qa/sessions/{id} |
| WebView 侧边栏 | 高 | 左侧历史对话列表，点击可加载历史消息 |
| 会话切换 | 高 | 点击历史对话 -> 加载消息 -> 渲染到聊天区 |
| 小程序端入口 | 中 | 个人中心对话记录 -> 继续对话 (带 session_id) |

### 3.3 头像上传

当前状态：profile.js 中 onChooseAvatar 调 POST /user/avatar，但后端无此接口。

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 头像上传接口 | 中 | POST /user/avatar，保存到 static/avatars/ |
| 前端流程 | 中 | chooseAvatar -> 压缩 -> 上传 -> 获取 URL -> PUT /user/profile |

### 3.4 语音识别 (ASR) ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 阿里云 Paraformer ASR | OK | 已接入，语音→文字识别正常 |
| 微信小程序录音 | OK | wx.getRecorderManager() 录制 MP3 → 上传后端 |
| WebView 浏览器录音 | OK | getUserMedia + MediaRecorder → fetch 上传 |
| 语音→LLM→TTS 全链路 | OK | 录音 → ASR识别 → LLM回答 → Edge TTS播报 |

### 3.5 真机测试与部署

| 任务 | 状态 | 说明 |
|------|------|------|
| 服务器部署 | ✅ | 47.118.21.150，Nginx + uvicorn :8001 |
| 域名 | ✅ | xinphotoshare.click |
| HTTPS 证书 | ✅ | Let's Encrypt，自动续期 |
| WebView 业务域名 | ✅ | xinphotoshare.click 已配置 |
| 阿里云 ASR | ✅ | Paraformer-v1，ffmpeg PCM 16kHz 转码 |
| 真机测试 | 待做 | 微信登录 -> GPS 签到 -> 语音问答 -> 对话连续性 |

### 3.6 精选景点完善

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 其他 4 个精选景点补全数据 | 中 | 目前只有灵山大佛有完整的参观信息/文化内涵/详细介绍 |
| 更多景点实拍图 | 低 | 目前覆盖 5/22 个景点 |
| 景点详情页增加图片展示 | 低 | 目前只有文字卡片 |

### 3.7 其他

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 天气集成 | ✅ | 心知天气(Seniverse) API，首页 Hero 显示实时温度/天气描述/图标。小程序直调服务器 `xinphotoshare.click/api/v1/weather/current`，其余 API 走本地 |
| 多语言 TTS | 低 | 前端切换 Edge TTS 语音 |
| Live2D 纹理优化 | 低 | 8192x8192 原图压缩为 WebP |
| WebSocket 实时通信 | 低 | 替代当前 URL hash 方式 |
| 小程序原生 Live2D | 低 | 替代 WebView，实现原生 UI 叠加 |
| 单元测试 | 低 | FastAPI + pytest |

---

## 四、新增 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /narration/spot-audio/{id} | TTS 生成景点语音讲解，返回 MP3 URL + 预估时长，缓存 24h |

## 五、已知局限

| 问题 | 影响 | 缓解 |
|------|------|------|
| 远程服务器已部署最新代码 | ✅ 已解决 | 47.118.21.150 (xinphotoshare.click) |
| WebView 全屏覆盖 | 无法叠加小程序原生 UI | 可接受；长期考虑 WebSocket+原生渲染 |
| WebView 刷新丢聊天 DOM | 切 tab 回来消息消失 | session_id 持久化到 localStorage |
| SQLite 单文件 | 不支持高并发 | MVP 可接受；生产环境迁移 PostgreSQL |
| 天气单独走服务器 | 首页天气调服务器，其他 API 走本地 | 开发阶段可接受 |

---

## 六、环境配置

### 后端 .env

WECHAT_APPID=wxa7ecb1e815ef6378
WECHAT_SECRET=81801db5ac5654138c3d050aa65ac0e6
DEEPSEEK_API_KEY=sk-768deef6d6c446d1886c52a9801c4795
DEEPSEEK_BASE_URL=https://api.deepseek.com
ALIBABA_ACCESS_KEY_ID=your_alibaba_access_key_id
ALIBABA_ACCESS_KEY_SECRET=your_alibaba_access_key_secret
ALIBABA_ASR_APPKEY=your_asr_appkey
SENIVERSE_API_KEY=your_seniverse_key

### 启动

cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

### 微信开发者工具

项目目录: miniprogram/
AppID: wxa7ecb1e815ef6378
开发阶段: 设置 -> 不校验合法域名

---

## 七、API 全览

### 用户 /api/v1/user

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /user/login | 微信登录 code -> openid -> token |
| POST | /user/check-token | 校验 token 签名和有效期 |
| GET | /user/profile | 获取用户资料 |
| PUT | /user/profile | 更新昵称/头像 |
| GET | /user/history | 签到记录 + QA 会话摘要 |

### 问答 /api/v1/qa

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /qa/ask | 语音/文字问答 multipart: audio+text+session_id+token |
| GET | /qa/sessions/{id} | 获取会话完整消息列表 |
| POST | /qa/feedback | 消息反馈 thumbs_up / thumbs_down |

### 景区 /api/v1

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /spots | 景点列表 |
| GET | /spots/{id} | 景点详情 + 相邻景点 |
| GET | /routes | 路线列表 |
| GET | /routes/{id} | 路线详情 + 途径景点序列 |
| POST | /narration/checkin | GPS 签到触发讲解 |
| POST | /narration/manual | 手动触发景点讲解 |
| GET | /narration/spot-audio/{id} | TTS 生成景点语音讲解（返回 MP3 URL + 时长） |
| GET | /tips/current | 当前智能提示 |
| GET | /weather/current | 实时天气（心知天气） |
| GET | /health | 健康检查 |

---

## 八、最近改动记录

### 2026-07-11 [语音讲解 & 地图修复]

#### 1. 语音讲解 — 真机/原生 APP 播放修复

| 问题 | 根因 | 修复 |
|------|------|------|
| 手机 APP 点击语音讲解无声音 | ① `project.miniapp.json` 中 Android `media: false` + iOS `WeAppMedia: false`，原生 APP 音频扩展 SDK 被禁用 ② `InnerAudioContext` 未设置 `obeyMuteSwitch = false`，iOS 静音键阻止播放 ③ 缓冲未完成时点播放静默失败，无重试机制 ④ 播放失败无任何用户提示 | ① `media` / `WeAppMedia` 设为 `true` ② `audioCtx.obeyMuteSwitch = false`（属性赋值方式，兼容所有微信版本） ③ 增加 `_wantsPlay` 标记：`onCanplay` 回调检测到用户曾点播放 → 自动补调 `play()` ④ `onError` → Toast + 红色错误文字 |

**`audio-player.js` 完整重写：**

| 改动 | 说明 |
|------|------|
| `obeyMuteSwitch = false` | 创建后立即设置属性（非构造函数传参，兼容 MiniApp SDK） |
| `_wantsPlay` 机制 | 用户点击播放 → 设置标记 → 先尝试 `play()` → 若缓冲中 → `onCanplay` 自动补播 |
| try-catch 包裹 `createInnerAudioContext` | MiniApp SDK 不兼容时显示友好提示，不崩溃 |
| 环境检测 | DevTools 用 `wx.downloadFile` 下载后播放；真机用 `audioCtx.src = url` 直接播放（避免 downloadFile 域名白名单） |
| 错误提示 | `onError` → `wx.showToast('音频加载失败')` + 界面红色 ⚠️ |
| 加载状态 | `onWaiting` → 按钮 ⏳ 脉冲动画 |
| 空指针保护 | 所有方法 `if (!audioCtx) return` |

**`project.miniapp.json` 修改：**
```diff
- "media": false       →  "media": true          (Android)
- "WeAppMedia": false  →  "WeAppMedia": true     (iOS)
```

**`requirements.txt`：** 新增 `edge-tts>=6.1.0`（TTS 依赖文档化）

**音频文件补全：** 本地生成 14 个缺失景点的 MP3 文件（LS-002~005、LS-010、LS-012~013、LS-015、NH-001~006），现共 22 个景点全部有预生成音频。

#### 2. 地图页面修复（4 个 Bug）

| Bug | 症状 | 根因 | 修复 |
|-----|------|------|------|
| NH 景点跳转错误 | 点击拈花湾景点跳到灵山景点详情 | `parseInt(s.id.split('-')[1])` 丢弃前缀，`LS-001` 和 `NH-001` 都映射为 markerId=1；`onMarkerTap` 硬编码 `LS-` 前缀 | `_toMarkerId()` 前缀编码：LS→1000+ / NH→2000+；`_spotIdMap` 映射表存储 markerId→spotId，点击时还原完整 ID |
| 地标名称不显示 | 改 `BYCLICK` 后需点击才出现 | 上轮改为 `BYCLICK` 解决重叠问题，但用户需要始终显示 | 恢复 `display: 'ALWAYS'`，同时缩小字号(11)/边距(4) + 提高缩放(16)减轻重叠 |
| 自定义图标缺失 | 红色地标无自定义图标 | `/images/marker-default.png` 文件不存在 | `iconPath: ''` 使用系统默认红色地标 |
| 地标名称挤成一排 | 密集区域 callout 重叠 | 灵山核心区 8 个景点集中在 0.003° 内 | `scale: 15→16` 多放大一级 + `fontSize: 13→11` / `padding: 6→4` 气泡缩小 |

**修改文件：** `miniprogram/pages/map/map.js`

#### 3. 修改文件清单

| 文件 | 改动类型 |
|------|----------|
| `miniprogram/components/audio-player/audio-player.js` | 重写（播放核心逻辑） |
| `miniprogram/components/audio-player/audio-player.wxml` | 更新（加载态/错误态 UI） |
| `miniprogram/components/audio-player/audio-player.wxss` | 更新（脉冲动画等样式） |
| `miniprogram/pages/map/map.js` | 修复（ID 映射 + 显示优化） |
| `miniprogram/project.miniapp.json` | 配置（启用 media SDK） |
| `backend/requirements.txt` | 依赖（新增 edge-tts） |
| `backend/audio/` (14 个新文件) | 资源（音频补全） |

### 2026-07-09 [本轮-3]

1. **天气服务切换为心知天气**
   - 和风天气 API Host 不匹配 → 换用心知天气(Seniverse) 免费 API
   - 新增 GET /api/v1/weather/current，返回实时温度/天气描述/图标
   - 15 分钟数据库缓存
   - 小程序首页直调服务器 `xinphotoshare.click`，其余 API 仍走本地后端

2. **微信登录升级为官方 getPhoneNumber**
   - "允许"按钮改为 `open-type="getPhoneNumber"`，弹出微信官方手机号授权框
   - 后端 `POST /user/login` 改为异步，接入阿里云手机号解密
   - User 模型新增 `phone` 字段

3. **首页封面修复**
   - LS-009 实为百子戏弥勒，LS-011 为灵山大佛，更正图片映射和卡片描述
   - 灵山大佛竖图 `object-position: center 28%` 顶部优先显示

### 2026-07-09 [本轮-2]

1. **服务器代码同步**
   - 47.118.21.150 服务器代码拉取到本地仓库
   - speech_client.py：Mock ASR → 真实阿里云 NLS Paraformer（Token/ffmpeg/Paraformer REST API）
   - config.py：alibaba_asr_appkey 替换旧的 alibaba_tts_voice + alibaba_asr_model
   - 新增 narration_service.py
   - build/live2d-viewer 前端同步
   - 确认服务器部署：Nginx HTTPS + uvicorn :8001
   - 确认域名 xinphotoshare.click（Let's Encrypt SSL）

### 2026-07-09 [本轮-1]

1. **首页大改版**
   - Hero Banner：Logo + 22景点/3路线/AI统计 + 天气角标
   - 快捷入口 2x2 网格（新增「我的」）
   - 精选景点横向滑动，5 张实拍图卡片
   - 游览贴士卡片（开放时间 + 建议时长）
   - 「查看全部」自动定位地图到景点区域

2. **景点详情语音讲解**
   - 始终显示音频播放器（加载中 → 就绪 → 不可用三种状态）
   - Edge TTS 生成 MP3 讲解，缓存 24 小时
   - 后端新增 GET /narration/spot-audio/{spot_id}
   - 播放器：播放/暂停 + 进度条 + 时长显示（外部传入预估时长，播放时真实修正）
   - 灵山大佛数据补全（参观信息/文化内涵/详细介绍/讲解文稿）

3. **用户登录改造**
   - 废弃 getPhoneNumber（个人小程序不可用）
   - 改为自定义授权弹窗，用户有「允许/拒绝」的授权体验
   - 后端预留了手机号解密逻辑（企业小程序时启用）

4. **资源文件**
   - 5 张景点实拍图：miniprogram/images/spots/
   - Logo.jpg 用于首页 Hero Banner

### 2026-07-08

1. **用户识别系统打通**
   - qa.py / narration.py / tips.py 全部接入 get_user_id()
   - user_id 从写死的 anonymous -> 真实用户 ID
   - 签到冷却按用户独立

2. **真实微信登录**
   - 新增 wechat_client.py: 调微信官方 API code -> openid
   - Token 升级为 HMAC-SHA256 签名，30 天过期
   - 新增 POST /user/check-token 接口
   - 新增 PUT /user/profile 接口

3. **登录体验优化**
   - 降级兼容新版微信
   - silentLogin 改为游客模式

4. **对话连续性**
   - WebView 端 session_id 持久化到 localStorage
   - 刷新页面后自动恢复会话上下文

5. **Bug 修复**
   - qa.py 语法错误修复

### 2026-07-10 [服务器部署 & Bug 修复]

#### 1. 精选景点数据修复

| 问题 | 根因 | 修复 |
|------|------|------|
| 首页精选景点不显示 | 服务器 `lingshan.db` 为空（0 景点 / 0 路线） | 上传本地数据库，22 景点 + 3 路线 |

同时修改 `main.py`：去除 `seed_if_empty()` 自动灌数据逻辑（与已有数据冲突），改为纯 `Base.metadata.create_all()`。

#### 2. 语音讲解修复（3 层问题）

| 层 | 问题 | 修复 |
|----|------|------|
| Nginx | `/static/audio/` 路由缺失，请求落到图库返回 HTML | `/etc/nginx/sites-enabled/photo-gallery` 增加 `location /static/audio/ { alias /opt/backend/audio/; expires 1d; }` |
| 音频格式 | Edge TTS 生成 MPEG v2 / 24kHz，微信 `wx.createInnerAudioContext()` 无法解码（报 `Unable to decode audio data`） | TTS 生成后增加 ffmpeg 转码步骤：`ffmpeg -i raw.mp3 -acodec mp3 -ar 44100 -ab 128k -ac 1 output.mp3` |
| 远程播放 | 微信开发工具对远程 HTTPS MP3 解码不稳定 | `audio-player` 组件改用 `wx.downloadFile()` 先下载到本地临时文件再播放 |

后端点：`GET /narration/spot-audio/{spot_id}` — Edge TTS 生成 → ffmpeg 转码 → 缓存 24h。

#### 3. 微信授权登录修复

| 问题 | 根因 | 修复 |
|------|------|------|
| 登录返回假 openid | 服务器 `.env` 中 `WECHAT_APPID=your_wechat_appid`（占位符） | 改为真实值 `wxa7ecb1e815ef6378` |
| `POST /user/login` 500 错误 | ① `app/utils/auth.py` 文件丢失 ② `User` 模型缺 `phone` 字段 ③ `WxLoginRequest` 缺 `phone_code` 字段 ④ 服务器缺 `wechat_client.py` | ① 从 git 恢复 ② `models/__init__.py` 增加 `phone` 列 ③ 补全 `schemas.py` ④ 上传缺失文件 |
| 头像上传不可用 | 后端无 `POST /user/avatar` 接口 | 新增接口：接收图片 → 保存到 `static/avatars/` → 更新用户 `avatar_url` |
| `auth.js` / `api.js` / `spot-detail.js` 硬编码旧 IP | `http://10.60.40.229:8001` | 统一改为 `https://xinphotoshare.click/api/v1` |

#### 4. 数字人手机黑影修复（Live2D）

| 问题 | 根因 | 修复 |
|------|------|------|
| 手机浏览器打开 Live2D 显示黑影 | ① 模型纹理 8192×8192（16.4MB），手机 GPU 爆显存 ② PixiJS 无移动端适配 | ① ffmpeg 压缩纹理到 2048×2048（1.5MB）② HTML 增加 `isMobile` 检测：降低分辨率、关闭抗锯齿、`powerPreference: 'high-performance'` |

#### 5. 小程序前端统一调整

| 改动 | 文件 |
|------|------|
| `apiBase` 改为 `https://xinphotoshare.click/api/v1` | `app.js` |
| 3 处 fallback URL 从旧 IP 改为服务器地址 | `utils/auth.js`, `utils/api.js`, `pages/spot-detail/spot-detail.js` |
| 音频播放器增加下载后本地播放逻辑 | `components/audio-player/audio-player.js` |
| 音频加载增加 console 诊断日志 | `spot-detail.js`, `audio-player.js` |

#### 6. 开发者工具 web-view 业务域名

- **临时**（开发）：详情 → 本地设置 → 勾选 ☑️ "不校验 web-view（业务域名）"
- **永久**（上线）：mp.weixin.qq.com → 开发管理 → 业务域名 → 添加 `xinphotoshare.click`，下载验证文件放服务器根目录
